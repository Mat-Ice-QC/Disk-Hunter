# Pinned with == to prevent supply chain attacks — unpinned deps can pull
# a compromised or yanked release without notice.
fastapi==0.135.3
uvicorn==0.44.0
python-multipart==0.0.24
pydantic==2.12.5
fpdf2==2.8.7
httpx==0.28.1
websockets==12.0
